[project]
name = "recover-cot"
version = "0.2.0"
requires-python = ">=3.9"
dependencies = ["PyYAML>=6.0.2,<7"]

[project.scripts]
recover-cot = "recover_cot:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["recover_cot"]
