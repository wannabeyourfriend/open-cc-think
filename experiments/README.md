# Experiments

Each hypothesis gets a directory containing a locked `protocol.md`, experiment-specific code only
when shared `src/` utilities do not suffice, sanitized results, and an `analysis.md`.

A protocol commit must precede its result commit. Record confirmatory outcomes separately from
exploratory follow-ups.
